import React, { useContext } from 'react'
import './Cart.css';

const Cart = () => {
  const{cartItems,food_list,removefromCart}= useContext(StoreC);

  return (
    <div></div>
  )
}

export default Cart